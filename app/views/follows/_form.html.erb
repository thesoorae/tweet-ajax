<% followed = current_user.follows?(user) ? "followed" : "unfollowed" %>

<button type="button" name="follow-button" class="FollowToggle" data-user-id="<%= user.id %>" data-initial-follow-state ="<%= followed %>"></button>
